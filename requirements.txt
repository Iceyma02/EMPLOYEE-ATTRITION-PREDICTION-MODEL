# requirements.txt - STREAMLIT CLOUD COMPATIBLE VERSION

# Core data science - USE PRE-COMPILED WHEELS
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.3.0
xgboost==1.7.6
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.15.0

# UI - Streamlit
streamlit==1.25.0

# API Client - For connecting to your Render API
requests==2.31.0

# REMOVED PROBLEMATIC PACKAGES:
# - fastapi (not needed for UI)
# - uvicorn (not needed for UI)
# - psycopg2-binary (REMOVED - requires PostgreSQL)
# - sqlalchemy (REMOVED - not needed for UI)
# - mlflow (REMOVED - not needed for UI)
# - -e . (REMOVED - causes path issues)
